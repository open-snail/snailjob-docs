---
title: 服务端配置信息
date: 2023-08-11 11:19:35
permalink: /pages/v3.x/c4285b/
---

<code-group>
  <code-block title="yaml风格" active>

```yaml
easy-retry:
  # 拉取重试数据的天数
  last-days: 30
  # 拉取重试数据的每批次的大小
  retry-pull-page-size: 100
  # 服务端netty端口
  netty-port: 1788
  # 重试和死信表的分区总数
  total-partition: 2
  # 一个客户端每秒最多接收的重试数量指令
  limiter: 10
  # 号段模式下步长
  step: 100
  # 日志保存时间(单位: day)
  log-storage: 90
  #当前使用的数据库(已废弃)
#  db-type: mysql
  # 负载均衡周期时间
  load-balance-cycle-time: 10
  # bucket的总数量    
  bucket-total: 128
  # 系统模式
  mode: all
  # 最大拉取次数
  retry-max-pull-count: 10
  # Dashboard 任务容错天数
  summary-day: 7
  # 回调配置
  callback:
    # 回调uniqueId前缀
    prefix: CB
    # 回调最大执行次数
    max-count: 288
    # 间隔时间
    trigger-interval: 900 

```
  </code-block>

  <code-block title="properties风格">

```java
# 配置拉取重试数据的天数
easy-retry.last-days=30
# 配置每批次拉取重试数据的大小
easy-retry.retry-pull-page-size=100
# 配置服务端 Netty 端口
easy-retry.netty-port=1788
# 配置重试和死信表的分区总数
easy-retry.total-partition=2
# 配置一个客户端每秒最多接收的重试数量指令
easy-retry.limiter=10
# 配置号段模式下的步长
easy-retry.step=100
# 配置日志保存时间（单位：天）
easy-retry.log-storage=90
    
# 回调配置
# 回调uniqueId前缀
easy-retry.callback.prefix=CB
# 配置回调的最大执行次数
easy-retry.callback.max-count=288
# 配置回调触发的间隔时间
easy-retry.callback.trigger-interval=900
    
# 配置当前使用的数据库类型
easy-retry.db-type=mysql
# 配置负载均衡周期时间
easy-retry.load-balance-cycle-time=10
# 最大拉取次数
easy-retry.    
# bucket的总数量    
easy-retry.bucket-total=128
# 系统模式
easy-retry.mode=all
# 最大拉取次数    
easy-retry.retry-max-pull-count=10
# Dashboard 任务容错天数
easy-retry.summary-day=7
```
  </code-block>

</code-group>

